import { AccountDashboard } from "@/components/account/AccountDashboard";

export const metadata = {
  title: "My Account | NeusomaHealing Practice",
};

export default function AccountPage() {
  return <AccountDashboard />;
}
